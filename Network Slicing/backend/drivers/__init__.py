"""
Placeholder drivers for ONOS, OpenDaylight, and ONAP.

These are implemented as async stubs so the rest of the application
can be wired up without requiring the actual controllers. Replace the
`asyncio.sleep` calls with real REST/NETCONF integrations for a
production deployment.
"""

